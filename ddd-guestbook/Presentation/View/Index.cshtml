<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8" />
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.0/css/bootstrap.min.css" integrity="sha384-9aIt2nRpC12Uk9gS9baDl411NQApFmC26EwAOH8WgZl5MYYxFfc+NcPb1dKGj7Sk" crossorigin="anonymous" />
    <link rel="stylesheet" href="static/css/index.css" />
    <script src="https://code.jquery.com/jquery-3.5.1.min.js" integrity="sha256-9/aliU8dGd2tb6OSsuzixeV4y/faTqgFtohetphbbj0=" crossorigin="anonymous"></script>
    <script src="https://cdn.jsdelivr.net/npm/vue/dist/vue.js"></script>
    <script src="static/js/index.js"></script>
    <title>Guestbook</title>
</head>
<body>
    <nav class="navbar navbar-extend navbar-dark bg-dark">
        <h3 class="text-light">Guestbook</h3>
    </nav>
    <div class="container">
        <div class="form-group row mt-4">
            <label class="col-2 col-form-label text-right">Name</label>
            <div class="col-10 col-sm-4">
                <input type="text" class="form-control" id="guestbook-name" value="anonymous" v-on:keypress.enter="post" />
            </div>
        </div>
        <div class="form-group row">
            <label class="col-2 col-form-label text-right">Message</label>
            <div class="col-10">
                <input type="text" class="form-control" id="guestbook-message" v-on:keypress.enter="post" />
            </div>
        </div>
        <div class="form-group row">
            <label class="col-2 col-form-label text-right">Lines</label>
            <div class="col-3 col-sm-2">
                <select class="form-control" id="guestbook-count" v-on:change="get">
                    <option value="10">10</option>
                    <option value="30">30</option>
                    <option value="50">50</option>
                </select>
            </div>
        </div>
        <div class="form-group row">
            <div class="col-2"></div>
            <div class="col-10">
                <input type="button" class="btn btn-primary" value="Post" v-on:click="post" />
            </div>
        </div>
        <div class="guestbook-posts mt-4" v-cloak>
            <div v-for="p in posts" class="d-flex">
                <div class="guestbook-posts-name ml-1 mr-1 text-nowrap d-flex align-items-start" v-bind:style="{color: p.color}">[[ p.name ]]</div>
                <div class="guestbook-posts-message ml-1 mr-1" v-bind:style="{color: p.color}">[[ p.message ]]</div>
                <div class="guestbook-posts-datetime ml-1 mr-1 mb-1 text-nowrap d-flex align-items-end">([[ toDatetime(p.timestamp) ]])</div>
            </div>
        </div>
    </div>
</body>
</html>
